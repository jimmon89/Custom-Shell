#!/usr/bin/env bash

source "$HOME/.__neutral__."

[[ -f "$(which pyenv)" ]] && eval "$(pyenv init - bash)"
[[ -f "$(which pyenv)" ]] && eval "$(pyenv virtualenv-init -)"
[[ ! -r '/home/jimmy/.opam/opam-init/init.sh' ]] || source '/home/jimmy/.opam/opam-init/init.sh' > /dev/null 2> /dev/null